version: '3.8'
networks:
  nginx_default:
    external: true
  backend:
    driver: bridge

services:
  backend:
    container_name: backend
    build:
      context: ./backend
    volumes:
      - ./backend:/app
    ports:
      - "8081:8000"
    env_file:
      - ./backend/.env
    networks:
      - backend

  frontend:
    container_name: frontend
    build:
      context: ./frontend
    ports:
      - "80:80"
    environment:
      - REACT_APP_BACKEND_URL=http://backend:8081
    networks:
      - nginx_default
      - backend
